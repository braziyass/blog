spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/blog
    username: postgres
    password: yassine2004
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update #create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
    database: POSTGRESQL
    database-platform: org.hibernate.dialect.PostgreSQLDialect

# Mail configuration for Mailtrap
  mail:
    host: live.smtp.mailtrap.io
    port: 587
    username: api
    password: "1b77139623339e174c6fd4039580abcb"
    # Use the Mailtrap example MAIL FROM for envelope-from and From header
    from: hello@demomailtrap.co
    # optional From display name used by EmailService
    from-name: "Brazi yassine"
    test-connection: true          # validate SMTP on startup
    properties:
      mail:
        debug: true
        smtp:
          auth: true
          starttls:
            enable: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000
          # envelope MAIL FROM used by SMTP server (must be an allowed sender)
          from: hello@demomailtrap.co