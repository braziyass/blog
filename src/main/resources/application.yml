spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/blog
    username: postgres
    password: yassine2004
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update #create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
    database: POSTGRESQL
    database-platform: org.hibernate.dialect.PostgreSQLDialect

  # Mail configuration for Mailtrap
  mail:
    host: live.smtp.mailtrap.io
    port: 587
    username: api
    password: "a8936a743484cedf4a88fa815b7d8033"
    from: no-reply@braziyas.dev
    from-name: "Brazi yassine"
    test-connection: true
    properties:
      mail:
        debug: true
        smtp:
          auth: true
          starttls:
            enable: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000
          from: no-reply@braziyas.dev

  # Redis configuration: use full URI (supports username:password in the URI)
  redis:
    # full URI form — keep quoted because password contains special characters
    url: "redis://default:SbXUlGaNJqadOuQGo9qC09ZEzovL2u3y@redis-17673.crce202.eu-west-3-1.ec2.cloud.redislabs.com:17673"